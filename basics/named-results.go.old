package main

import "fmt"

func split(sum int) (x, y int) {
	// x and y are named as integer return values.
	// when return is called return x and y
	x = sum * 4 / 9
	y = sum - x
	return
}

func main() {
	fmt.Println(split(17))
}
